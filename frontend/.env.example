# Copy to .env and fill in real values. Never commit .env.

# Base URL of the API, including the /api/v1 prefix.
# Optional: when unset, apiClient.js falls back to http://localhost:5002/api/v1
# in development and the deployed Render API in production.
VITE_API_BASE_URL=http://localhost:5002/api/v1
